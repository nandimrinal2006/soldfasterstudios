on: push
name: Deploy website on push
jobs:
  web-deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
    - name: Get latest code
      uses: actions/checkout@v3
    
    - name: Sync files
      uses: nandimrinal2006/collected_public_repositories/tree/master/FTP-Deploy-Action-master/FTP-Deploy-Action-master
      with:
        server: ftp.mazurstudios.com
        username: devusermz@mazurstudios.com
        password: xujDnebyd0Np
